import { GlobalAnalyticsContent } from './GlobalAnalyticsContent';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Аналитика — StroyDocs' };

export default function AnalyticsPage() {
  return <GlobalAnalyticsContent />;
}
